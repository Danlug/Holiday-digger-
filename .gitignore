.godot/
.import/
export.cfg
# export_presets.cfg НАМЕРЕННО отслеживается (не игнорируется): задание прямо
# просит завести пресеты Web/iOS как часть репозитория (см. docs/BUILD.md).
# В нём нет секретов — реальные Team ID/провижининг заполняются локально
# разработчиком перед архивацией iOS, в репозитории только заглушки.
*.translation
build/
.DS_Store
